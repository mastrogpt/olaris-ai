version: 3
tasks:
  run:
    silent: true
    require: [S]
    desc: run script
    cmds:
     - |
       if test -z "{{.S}}"
       then echo -e "Usage: run S=<script> [A=<args>]\nScripts:"
            ls *.py | awk '{gsub(/.py/, ""); print "- " $0}'
       else python {{.S}}.py {{.A}}
       fi
